---
import { Image } from "astro:assets";

import Social from "@/components/Social.astro";

import LinkedInIcon from "@/icons/LinkedIn.astro";
import GithubIcon from "@/icons/Github.astro";
import MailIcon from "@/icons/Mail.astro";

import cvImage from "@/assets/cv.jpg";

const SOCIAL_LINKS = [
  {
    href: "https://github.com/vekio/",
    label: "GitHub",
    icon: GithubIcon,
  },
  {
    href: "https://www.linkedin.com/in/alberto-castaneiras/",
    label: "LinkedIn",
    icon: LinkedInIcon,
  },
  {
    href: "mailto:root@vekio.dev",
    label: "root@vekio.dev",
    icon: MailIcon,
  },
];
---

<section
  id="hero"
  class="px-6 py-16 md:py-24 lg:min-h-screen lg:flex lg:items-center lg:justify-center"
>
  <div
    class="mx-auto grid max-w-6xl items-start gap-12 md:gap-16 lg:grid-cols-[minmax(0,1.35fr)_minmax(0,0.65fr)]"
  >
    <div class="space-y-8">
      <div>
        <h1 class="shimmer-text text-5xl font-bold text-balance lg:text-7xl">
          Alberto Castañeiras
        </h1>
        <p class="mt-2 text-xl text-muted-foreground lg:text-2xl">
          Ingeniero de Software
        </p>
      </div>

      <div
        class="text-lg text-muted-foreground leading-relaxed max-w-lg space-y-4"
      >
        <p>
          👋 Buen diseño. Código limpio. Software que funciona. Curioso y en
          aprendizaje constante, combino experiencia en desarrollo web con <strong
            >.NET</strong
          >,
          <strong>Angular</strong> y BI con <strong>Qlik</strong>.
        </p>
        <p>
          Me apasionan <strong>Go</strong>, la automatización con <strong
            >IA</strong
          > y el
          <strong>self-hosting</strong>. Mantengo un <strong>homelab</strong> donde
          experimento, rompo cosas y aprendo cómo funcionan de verdad.
        </p>
      </div>

      <div class="flex flex-wrap items-center gap-3 pt-4">
        {SOCIAL_LINKS.map((social) => <Social {...social} />)}
      </div>
    </div>

    <div class="hidden lg:flex justify-end">
      <div class="relative group">
        <div
          class="absolute -inset-1 rounded-2xl bg-gradient-to-r from-primary/50 to-primary/20 blur-xl opacity-75 transition duration-500 group-hover:opacity-100"
        >
        </div>

        <div
          class="relative rounded-2xl border border-border bg-card p-6 shadow-2xl"
        >
          <div
            class="aspect-[3/4] w-[14.5rem] overflow-hidden rounded-xl xl:w-[16.75rem]"
          >
            <Image
              src={cvImage}
              alt="Currículum de Alberto Castañeiras"
              class="h-full w-full object-cover"
              loading="lazy"
              decoding="async"
              format="webp"
            />
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
